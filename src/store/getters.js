export default {
    isPaused: ({ isPaused }) => isPaused,
    gameOver: ({ gameOver }) => gameOver,
    fallingItem: ({ fallingItem }) => fallingItem,
    fallingItems: ({ fallingItems }) => fallingItems,
    gameTimeout: ({ gameTimeout }) => gameTimeout,
    score: ({ score }) => score,
    rotation: ({ rotation }) => rotation,
}